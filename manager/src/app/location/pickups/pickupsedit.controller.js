(function() {
    'use strict';
    angular.module('bProject').controller('PickupsEditController', PickupsEditController);

    function PickupsEditController(toastr, PickupsService, $log, $stateParams) {
        /*var vm = this;
        vm._id = $stateParams.id;
        PickupsService.GetById(vm._id).then(function(data) {
            vm.pickups = data;
            vm.loading = false;
        }, function(error) {
            $log.debug('error:' + error);
        });*/
    }
})();
